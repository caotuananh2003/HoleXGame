using UnityEngine;

/// <summary>
/// Deprecated stub — thay thế bởi FrameDefinition trong /Profile.
/// Giữ lại để không phá vỡ PlayerProfile SO serialization (AvatarFrameDefinitionList).
/// Không dùng class này trong code mới.
/// </summary>
[CreateAssetMenu(fileName = "AvatarFrameDefinition", menuName = "Scriptable Objects/AvatarFrameDefinition (Legacy)")]
public class AvatarFrameDefinition : ScriptableObject
{
}
